import {
    APolimorphicLib
} from "./polimorphiclib"

export default class HorizontalLib extends APolimorphicLib
{
    // Should only implement THE HORIZONTAL FUNCTIONS
    // Other can be let abstract
}