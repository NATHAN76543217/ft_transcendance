{
    "Servers": {
        "1": {
            "Name": "Main",
            "Group": "Servers",
            "Port": $POSTGRES_PORT,
            "Username": "$POSTGRES_USER",
            "Host": "$POSTGRES_HOST",
            "SSLMode": "prefer",
            "MaintenanceDB": "postgres",
            "PassFile": "$PGADMIN_PASSFILE"
        }
    }
}
