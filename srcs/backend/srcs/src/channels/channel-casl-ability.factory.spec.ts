import { ChannelCaslAbilityFactory } from './channel-casl-ability.factory';

describe('ChannelCaslAbilityFactory', () => {
  it('should be defined', () => {
    expect(new ChannelCaslAbilityFactory()).toBeDefined();
  });
});
