import { IUser } from "./IUser";
/* import { ChannelRelationship } from "./ChannelRelationship";
export type MappedChannel = ChannelRelationship & {
  name: string;
};
 */

// This is a placeholder that can be extended in the future
export type AuthenticatedUser = IUser;
